import { Gradient } from './Gradient.js'

// Create your instance
const gradient = new Gradient()

// Call `initGradient` with the selector to your canvas
const mesh = () => gradient.initGradient('#gradient-canvas')

export default mesh